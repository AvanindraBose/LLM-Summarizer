version: "3.9"

services:
  mlflow:
    image: python:3.10
    command: mlflow ui --host 0.0.0.0 --port 5000
    ports:
      - "5000:5000"

  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    depends_on:
      - mlflow